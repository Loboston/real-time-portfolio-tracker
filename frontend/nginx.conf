server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    # Serve React app for all other routes (ALB handles /api and /ws routing)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
